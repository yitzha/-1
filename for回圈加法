#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int i,sum=0;
    int num[32];
    for (i=0;i<5;i++){
        scanf("%d", &num[i]);
        sum= sum+num[i];
    }
    printf("the sum is %d", sum);
    return 0;
}