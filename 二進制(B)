#include <stdio.h>
#include <stdlib.h>

int main(void)
{
    int x,i;
    scanf("%d",&x);
    i = 1;
    while (i<x){
        i =i*2;
    }
    i= i/2;
    printf("%d\n",i);

    while(x>0){
            printf("%d", x/i);
            x= x%i;
            i= i/2;
    }

    return 0;
}