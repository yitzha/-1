#include <stdio.h>
int main(void)
{
    int a, b, c, d;
    scanf("%d %d %d %d%d", &a,&b, &c, &d);
    if (b-a == d-c && d-c == b-c){
        printf("%d%d", d+ d-c, )
    }
    printf("%d", z);
    return 0;
}
